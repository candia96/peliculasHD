<section>
  <h1 class="titulo-h2">Películas</h1>
 <article > 
  <table class="">
    <thead>
      <tr>
        <th>Titulo</th>
        <th>Director</th>
        <th>Critica</th>
        <th>Sinopsis</th>
        <th>Formato</th>
        <th>Torrent</th>
        <th>Sub</th>
        <th>Img</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @peliculas.each do |pelicula| %>
        <tr>
          <td><%= pelicula.titulo %></td>
          <td><%= pelicula.director %></td>
          <td><%= pelicula.critica %></td>
          <td><%= pelicula.sinopsis %></td>
          <td><%= pelicula.formato %></td>
          <td><%= pelicula.torrent %></td>
          <td><%= pelicula.sub %></td>
          <td><%= pelicula.img %></td>
          <td><%= link_to 'Show', pelicula %></td>
          <td><%= link_to 'Edit', edit_pelicula_path(pelicula) %></td>
          <td><%= link_to 'Destroy', pelicula, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>
</article>
</section>